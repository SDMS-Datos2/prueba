/* 
 * File:   darray.cpp
 * Author: justin
 *
 */
#include "darray.h"
#include "dheap.h"
/**
 * 
 * @param pPunterodHeap
 */
dArray::dArray(void* pPunterodHeap)
{
    punteroHeap = pPunterodHeap;
}

/**
 * @brief dArray::operator = Operador de asignacion que
 * almacena un arreglo remotamente y lo borra de la
 * memoria local.
 * @param pArray es un arreglo.
 * @return un puntero tipo void.
 */
//void* dArray::operator =(const array pArray){
  //  int _dato = pInt;
    //d_pointer_size_type _puntero;
    //_puntero= ((dHeap*)punteroHeap)->dMalloc(_dato, "String");
    //((dHeap*)punteroHeap)->dnew(_puntero,_dato);
//}
//Debo seguir e
